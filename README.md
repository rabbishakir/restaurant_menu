# Menu Builder (Next.js + TypeScript)

Boilerplate Next.js App Router project for a Restaurant Menu Builder.

Getting started:

```bash
npm install
npm run dev
```

This repository includes a minimal app layout, a `/login` page, an `/admin` dashboard, an `/admin/menus` list page, and an `/admin/menus/[id]` edit page — all placeholders. No backend logic yet.
