generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

enum MenuStatus {
  DRAFT
  PUBLISHED
}

enum MenuItemType {
  ITEM
  CATEGORY
}

model Menu {
  id                  String     @id @default(cuid())
  title               String
  status              MenuStatus @default(DRAFT)
  titleFontSize       Int        @default(48)
  itemFontSize        Int        @default(18)
  contentTopOffset    Int        @default(120)
  contentWidth        Int        @default(700)
  overlayOpacity      Int        @default(20)
  zelleImagePath      String?
  zelleX              Int        @default(50)
  zelleY              Int        @default(50)
  zelleWidth          Int        @default(120)
  contactImagePath    String?
  contactX            Int        @default(200)
  contactY            Int        @default(50)
  contactWidth        Int        @default(120)
  backgroundImagePath String?
  createdAt           DateTime   @default(now())
  updatedAt           DateTime   @updatedAt
  menuItems           MenuItem[]
  qrCodes             QrCode[]
}

model MenuItem {
  id       String       @id @default(cuid())
  menuId   String
  name     String
  price    String?
  type     MenuItemType @default(ITEM)
  position Int          @default(0)
  menu     Menu         @relation(fields: [menuId], references: [id], onDelete: Cascade)

  @@index([menuId])
}

model QrCode {
  id        String @id @default(cuid())
  menuId    String
  label     String
  value     String
  positionX Int    @default(50)
  positionY Int    @default(50)
  size      Int    @default(120)
  menu      Menu   @relation(fields: [menuId], references: [id], onDelete: Cascade)

  @@index([menuId])
}
